query GameList {
    gamesCollection {
        edges {
            node {
                id
                created_at
                name
                expires
                #thingsConnection {
                #    totalCount
                #}
                #playersConnection {
                #    totalCount
                #}
            }
        }
    }
}
